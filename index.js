// Write your code in this file!

// Import necessary functions from helpers.js if needed

// Define currentUser
const currentUser = 'Alice'; // Replace 'Alice' with the desired current user name

// Define welcomeMessage
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define excitedWelcomeMessage
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Define shortGreeting
const firstInitial = currentUser[0];
const shortGreeting = `Welcome, ${firstInitial}!`;
